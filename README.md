# KasBullet

Institutional Intelligence for Kaspa.

This repository is the clean Phase 1 rebuild foundation. It currently contains
only the blank project shell and brand asset.

## Run locally

Serve the repository root with any static server, for example:

```bash
python -m http.server 8000
```

Then open `http://localhost:8000`.
